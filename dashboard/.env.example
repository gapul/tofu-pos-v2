# Cloudflare Pages の Environment variables (Production / Preview の両方) に
# 同じキー名で登録してください。anon key はクライアントに同梱される公開鍵で、
# 行レベルセキュリティ (RLS) でデータを保護する前提です。
# 絶対に service_role キーは入れないこと。
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
